## Summary

What changed and why. Link any related issue.

## Checklist

- [ ] `ruff check src tests` passes
- [ ] `mypy src` passes
- [ ] `pytest tests/ -q` passes
- [ ] New operators have a parity test parametrized over `mlpackage` and `mlmodel`
- [ ] `CHANGELOG.md` updated under `[Unreleased]` if behavior or coverage changed
